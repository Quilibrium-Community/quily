app = "quily-bot"
primary_region = "fra"

[build]
  dockerfile = "bot/Dockerfile"

[env]
  NODE_ENV = "production"
  DISCORD_DAILY_LIMIT = "200"
  MAX_MESSAGE_LENGTH = "2000"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
  auto_stop = false
